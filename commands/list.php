<?php
require_once 'global/useTaskStore.php';
function getTaskListForShow()
{
    // $taskList = getTaskList();
    // var_dump(array_map(fn(Task $task) => "Название $task->description ", $taskList));
    return '';
}
